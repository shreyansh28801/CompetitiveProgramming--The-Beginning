#include <bits/stdc++.h>
using namespace std;

const int n=1e7+10;
int arr1[n];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n1=1e5+67;
    int arr[n1];
    
    /*

    For Cometitive Programming

    1. locally (inside any function i.e. included main) we can declare arary of size  of order 10^5  

    2. but globally , we can declare arary of size  of order 10^7  and size must be of const variable
    */

    return 0;
}